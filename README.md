Build: dotnet build

Run: SysTrayMenu.exe [folder]

Description: View folder as a menu, accessed via the system tray.

1. Click the menu item to run a file.
2. Hold down the Ctrl key to run multiple files sequentially without closing the menu.
3. Right-click the menu item to display it in File Explorer.
